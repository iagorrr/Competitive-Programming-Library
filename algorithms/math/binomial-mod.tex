\subsection{Binomial MOD}

Precompute every factorial until $maxn$ ($O(maxn)$) allowing to answer the $\binom{n}{k}$ in $O(\log{mod})$ time, due to the fastpow. Note that it needs $O(maxn)$ in memory.
