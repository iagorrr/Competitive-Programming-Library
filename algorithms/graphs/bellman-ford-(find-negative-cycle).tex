\subsection{Bellman-Ford (find negative cycle)}

Given a directed graph find a negative cycle by running $n$ iterations, and if the last one produces a relaxation than there is a cycle.

Time: $O(V \cdot E)$
