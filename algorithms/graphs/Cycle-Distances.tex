\subsection{Cycle Distances}

Given a vertex $s$ finds the longest cycle that end's in $s$, note that the vector \textbf{dist} will contain the distance that each vertex $u$ needs to reach $s$.

Time: $O(N)$

