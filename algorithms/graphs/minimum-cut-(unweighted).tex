\subsection{Minimum Cut (unweighted)}

After build the \textbf{direct/undirected} graph find the minimum of edges needed to be removed to make the sink $t$ unreachable from the source $s$.

Time: $O(V \cdot E^2)$
