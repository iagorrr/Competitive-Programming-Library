\subsection{Binary Liftign/Jumping}

Given a function/successor answers queries of the form which is the node after $k$ moves starting from $u$.

Time: build $O(N \cdot MAXLOG2)$, query $O(MAXLOG2)$.
